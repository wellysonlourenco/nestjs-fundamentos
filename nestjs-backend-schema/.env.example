# # Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=mydb_dev

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/mydb_dev?schema=public"

# Server
PORT=3000
NODE_ENV=development

# CORS
CORS_ORIGIN=http://localhost:3000,http://localhost:4200

# JWT
JWT_SECRET=sua_chave_secreta_super_segura
JWT_EXPIRES_IN=1d

# Upload Paths (opcional - usa default se não definir)
UPLOAD_PATH_AVATARS=./uploads/avatars
UPLOAD_PATH_DOCUMENTS=./uploads/documents
UPLOAD_PATH_VIDEOS=./uploads/videos
UPLOAD_PATH_GALLERY=./uploads/gallery

# Upload Limits
MAX_AVATAR_SIZE=5242880
MAX_DOCUMENT_SIZE=10485760
MAX_VIDEO_SIZE=52428800